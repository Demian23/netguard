#ifndef PORT_SCANNER_DEF
#define PORT_SCANNER_DEF
/*
#include "scheduler.h"

class PortScanner : public Task{
public:
    enum ScanMode{All};
    PortScanner(in_addr dest, ScanMode mode = All);
    bool Execute() override;
    virtual ~PortScanner();
private:
};
*/
#endif // !PORT_SCANNER_DEF
